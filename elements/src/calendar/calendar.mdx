import { Canvas, Meta, Story } from '@storybook/blocks';

import * as Calendar from './calendar.stories';

<Meta of={Calendar} />

# Calendar

<Canvas of={Calendar.Primary} />
<Canvas of={Calendar.Mini} />