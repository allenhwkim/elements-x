import { Canvas, Meta, Story, Controls } from '@storybook/blocks';

import * as ListStories from './list.stories';

<style>{`
  .error.required ~ *::after {
    display: block;
    content: 'A list item selection is required';
    color: red;
  }
`}</style>

<Meta of={ListStories} />

# List 

Convert unordered lists to a coolapsable and selectable.

Use arrow keys to navigate, space key to expand, and Enter key to select.

<Canvas of={ListStories.Primary}/>

<Controls />