import { Canvas, Meta, Story, Controls } from '@storybook/blocks';

import * as InputTable from './input-table.stories';

<Meta of={InputTable} />

# Input Table

Multiple input as an array or an object.  
Press 'Enter' to add a new row, and 'Backspace' on empty row to delete a row.

<Canvas of={InputTable.Primary}/>

<Controls />

### Object
<Canvas of={InputTable.Object}/>

### Strings with value
<Canvas of={InputTable.StringsWithValue}/>

### Objects with value
<Canvas of={InputTable.ObjectsWithValue}/>