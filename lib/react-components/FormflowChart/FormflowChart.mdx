import { Canvas, Meta, Story } from '@storybook/blocks';
import * as FormflowChartStories from './FormflowChart.stories';

<Meta of={FormflowChartStories} />

The React component used by form-diagram.
It provides functionality of interactivity with nodes and edges by providing initial nodes and edges as well as
 * `onNodeClick(node, nodes, edges)` function
 * `onEdgeClick(edge, nodes, edges)` function
 * `onInit(reactFlowInstance)` function

<Canvas of={FormflowChartStories.Primary} />