import { Canvas, Meta, Story, Controls } from '@storybook/blocks';

import * as InputFile from './input-file.stories';

<Meta of={InputFile} />

# Input (for files)

`<input type="file" />` alternative.
This will enable you to select a file and display preview of the file.

<Canvas of={InputFile.Primary}/>

<Controls />

<h2 className="h6 sb-unstyled fw-light">STORIES</h2>

### Message 
<Canvas of={InputFile.Message}/>
